apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/puja255/softwares:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/puja255/softwares:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: softwares
        chartPath: charts/softwares
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/puja255/softwares'
          image.tag: '{{.DIGEST_HEX}}'
